{{page-title "ShowTicket"}}
<br><br>

<center>

{{!-- <form {{action "InvoiceGenerate" on="submit" }}> --}}
<table id='tickets' style="width: ; border: 3px solid black; padding:10px; margin: 10px; ">
{{#each this.model as |seats| }}

{{#if seats.available }}
<button style="color:#2cd223; padding:10px; margin-right:10px; margin-bottom:10px; width: 3rem;"  id="{{seats.id}}" value="{{seats.seat_cnt}}" type="button" {{ action "invoice" seats.id }}>{{seats.seat_cnt}}</button>
{{else}}
<button style="pointer-events: none;color:#FF3333; padding:10px; margin-right:10px; margin-bottom:10px; width: 3rem;" id="{{seats.id}}" value="{{seats.seat_cnt}}" type="button" {{ action "invoice" seats.id }}>{{seats.seat_cnt}}</button>
{{/if}}
{{#if seats.seat_no_layout}}
<br>
{{/if}}
{{#if seats.bus_walk_place}}
<br><br>
{{/if}}

{{/each}}
</table>

{{!-- </form> --}}

{{#each this.model as |seats| }}

{{#if seats.slepper40}}
<table>

</table>
{{/if}}

{{/each}}

<div>
    <br>
<svg width="170" height="70">
  <rect x="50" y="20" width="40" height="40" style="fill:#175806;stroke:#175806;stroke-width:5;fill-opacity:0.5;stroke-opacity:0.9" />
</svg>

    <svg width="130" height="70">
  <rect x="50" y="20" width="40" height="40" style="fill:#E33C13;stroke:#E33C13;stroke-width:5;fill-opacity:0.5;stroke-opacity:0.9" />
</svg>
</div>
<center>Available &emsp;&emsp;&emsp;&emsp;&emsp;&emsp; Booked</center><br><br>
    <center><a href="#" onclick="javascript:window.history.back(-1);return false;">Back</a></center>
</center>
